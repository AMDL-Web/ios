# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.moved-aside
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/

# Claude Code local config — but share the skills, they document the build
.claude/*
!.claude/skills/

# Codex local config
.codex/

# Skills tooling (e.g. ShipSwift recipe library installed via `npx skills add`)
.agents/
skills-lock.json

# Local portal host. Config/Portal.xcconfig IS committed (empty) so the
# project's base configuration reference resolves in a fresh clone; it
# optionally includes this file, which holds your real host.
Config/Portal.local.xcconfig
